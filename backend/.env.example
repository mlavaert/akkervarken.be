# Database (Railway sets this automatically when you add PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# CORS - Add your domain(s)
ALLOWED_ORIGINS=https://akkervarken.be,http://localhost:1313

# Email Configuration (for order confirmations)
SMTP_HOST=xxx
SMTP_PORT=587
SMTP_USER=bestellingen@akkervarken.be
SMTP_PASSWORD=your-email-password
FROM_EMAIL=bestellingen@akkervarken.be
ADMIN_EMAIL=info@akkervarken.be
