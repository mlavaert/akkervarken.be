# Railway will set DATABASE_URL automatically when you add PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/database

# Add your domain (Railway will use this for CORS)
ALLOWED_ORIGINS=https://akkervarken.be,http://localhost:1313
